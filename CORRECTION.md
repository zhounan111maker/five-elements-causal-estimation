# 更正声明 / Correction Notice

**仓库**：`five-elements-causal-estimation`
**日期**：2026-08-31（2026-09-22 补充正文标注）
**状态**：本仓库已归档（read-only），核心结论**撤回**

---

## 1. 被撤回的结论

本仓库此前在 README 与论文中声称：

> 将五行特征工程与 DML 结合，使 ATE 估计的偏差降低 **89.8%**（银行场景）、**92.3%**（电商场景）。

**该结论不成立，已撤回。**

---

## 2. 根本原因：100% 目标泄漏（target leakage）

派生特征 `wood_trans_efficiency`（"木·交易效率"）的定义为：

```
wood_trans_efficiency = Total_Trans_Amt / Credit_Limit
```

其中 `Total_Trans_Amt` 正是构造结果变量 Y(0) 所用的原始列：

```python
df['outcome'] = df['Total_Trans_Amt'] * (1 + 0.15 * df['treatment'])
```

也就是说，该派生特征**直接由结果变量构造**，泄漏了 Y(0) 的信息。

关键点：**删除原始列 `Total_Trans_Amt` 并不能消除泄漏** —— 泄漏是通过派生特征进入 DML 协变量矩阵的。DML 的两阶段估计（`Y = g(X) + ε`、`D = m(X) + η`）中，只要 X 含 Y(0) 的函数，残差化就无法剥离这层关联。

---

## 3. 诊断证据：受控消融实验

设计：**只切换泄漏通道**（参数 α 控制派生特征是否含 Y(0) 信息），其余数据、DGP、模型、交叉拟合折数全部不变，重复 30 个随机种子。

| 配置 | ATE 偏差 | 估计量标准差 | 胜出种子数 | 统计检验 |
|------|---------|-------------|-----------|---------|
| 含泄漏特征（α = 1） | **−74%** | 40.59 → **10.15（塌缩 4.0×）** | 27/30 | — |
| 清洁特征（α = 0，无 Y(0) 信息） | **+2.3%** | 正常波动 | 15/30 | 配对 t 检验 **p = 0.892**（原版 vs 清洁版无差异） |

**两个关键读数：**

1. **清洁配置下五行特征没有任何显著增益**（p = 0.892，15/30 约等于抛硬币）。所谓"降低 89.8%"完全来自泄漏。
2. **泄漏的可识别指纹不是偏差下降，而是方差塌缩**（4.0×，报告的 SE 塌缩 4.65×）。这一点比"结果太好"更容易被误读为"方法更稳健"，是这类错误最危险的地方。

---

## 4. 附带发现的第二处错误：真值定义

原代码将 ATE 真值取为**对照组均值**（637.27）：

```python
true_ate = df.loc[df.treatment == 0, 'outcome'].mean()      # 637.27  ← 错误
```

正确处理效应的真值应为**全样本上的处理效应均值**：

```python
tau = 0.15 * df['outcome'].mean()   # 或在全样本上取 E[Y(1) - Y(0)]
    = 0.15 * E[Y(0)] = 660.61                                    # 正确
```

因干预为恒定乘性效应（×1.15），此错误使基准模型自带 **−3.53%** 的偏差。虽然它不改变"泄漏是主因"的结论，但会污染所有以真值为分母的"偏差降低率"指标。

---

## 5. 什么是对的 / 什么被推翻了

| 事项 | 结论 |
|------|------|
| DML + 交叉拟合 + 集成学习器这套**工程实现** | 可用，代码本身无逻辑错误 |
| "五行平衡度能显著提升 ATE 估计精度" | ❌ **被推翻**（清洁配置无显著差异） |
| "89.8% / 92.3% 偏差降低率" | ❌ **被撤回**（泄漏伪影） |
| "跨域泛化到电商场景" | ❌ **被推翻**（同一泄漏通道重复生效） |
| "五维验证体系" | ⚠️ **不足以证伪**（缺少最关键的一项：泄漏审计） |

---

## 6. 正确的做法（本仓库缺的那一步）

在把任何派生特征送进因果模型之前，逐个问：

1. **该特征的计算式里是否出现结果变量 Y（或其任何变换/分箱/滞后）？** 出现即泄漏。
2. **删掉原始列之后，该特征是否仍然携带 Y 的信息？** 若是，删列是假的"清洗"。
3. **对每个特征做单特征 AUROC / 与 Y 的相关系数排序**，异常高的（如接近 1）几乎必定是泄漏。
4. **检查估计量方差**：真实的方法改进通常小幅降偏差、方差平稳；泄漏会给"偏差骤降 + 方差塌缩"。

完整可复现的诊断流水线见新仓库。

---

## 7. 后续处理

- 本仓库 README 顶部已加显著更正声明，正文所有失效数字已就地标注（删除线 + ⛔）。
- 本仓库已**归档**（archived，read-only），保留作为透明更正记录。
- 纠正后的**负结果诊断研究**（含 30 种子受控消融、可复现脚本、Jupyter 演示、修正后的论文）已发布至：
  **https://github.com/zhounan111maker/dml-target-leakage-diagnosis**

## 8. 引用

请**不要**再引用本仓库的原始结论。如需引用此更正与诊断工作：

```
Zhou Nan (2026). A Controlled Diagnostic of Target Leakage in DML Covariates.
GitHub. https://github.com/zhounan111maker/dml-target-leakage-diagnosis
```

## 9. 联系

作者：周南 · 邮箱：3357424029@qq.com

---

*这份声明记录的是一次自查纠错：结论错了就是错了，比维持一个好看的数字更重要。*
